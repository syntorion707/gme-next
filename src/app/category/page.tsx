import React from 'react';
import CategoryPage from '@/components/categories-page';

const category = () => {
    return (
        <div>
            <CategoryPage />
        </div>
    );
};

export default category;
